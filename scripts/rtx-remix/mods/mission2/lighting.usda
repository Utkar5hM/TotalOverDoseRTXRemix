#usda 1.0
(
    customLayerData = {
        string lightspeed_layer_type = "replacement"
    }
    upAxis = "Y"
)

over "RootNode"
{
    # The current shadow-casting sun is injected by scripts/TODCameraResend.asi
    # as an enabled D3D9 directional light. Remix converts that runtime light to
    # a distant light. This layer stays empty until we author Toolkit-native
    # replacement lights.
}
