# Force managed textures to upload/hash on unlock before Remix evaluates RTX draw calls.
# Present in the last build where mission 3 raytraced correctly; restored after a
# regression where mission 3 lost raytracing.
d3d9.evictManagedOnUnlock = True
