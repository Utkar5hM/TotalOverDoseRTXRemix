# Archived Source Notes

This archive keeps the original long-form Markdown notes that used to sit at the top of `re_docs/`.

They are preserved for exact history, tables, hashes, and audit trails. They are intentionally not the first reading path anymore; use the compact top-level docs first:

- `../README.md`
- `../01_game_reverse_engineering_outputs.md`
- `../02_work_log_what_worked.md`

The files in `source_notes/` may contain old commands, launch instructions, repeated "next test" prompts, and historical names such as `TODCameraResend`. Treat those as source history, not necessarily the current package layout.
